package Selenium16thMarch2024;

import org.testng.Assert;
import org.testng.annotations.Test;

public class TC03 {

    @Test
    public void vwoLogin(){

        // system.property to setup the path of the Drivers before Selenium4
        // Chrome -> ChromeDriver
        // Edge -> EdgeDriver
        // Firefox -> GeckoDriver
        // Safari -> Safaridriver
        Assert.assertTrue(true); //Main method- TestNG
    }
}
